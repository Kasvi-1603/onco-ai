/**
 * Rich mock SessionPayload for View 1 development before API is ready.
 * Owner: FE Dev — Day 0
 */

import type { SessionPayload } from "./types";

export const mockSessionPayload: SessionPayload = {
  session_id: "demo-session-001",
  status: "pending",
};
